# V5 memory plugin runtime dependencies.
# Install into a dedicated venv (see install.py / install.sh):
#   python -m venv .venv && .venv/Scripts/python -m pip install -r requirements.txt
chromadb>=0.5.0
httpx>=0.27.0
mcp>=1.2.0
python-dotenv>=1.0.0
psutil>=5.9.0
